import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs'
import { aetherStoryDocs } from 'aetherspace/schemas/utils'
import { isEmpty } from 'aetherspace/utils/commonUtils'
// -i- Auto generated with "yarn document-components"
// Components hooking into the storybook auto-documentation script
import { default as BackIcon, getDocumentationProps as getBackIconProps } from '../../../../../features/app-core/icons/BackIcon.tsx'
import { default as ExpoIcon, getDocumentationProps as getExpoIconProps } from '../../../../../features/app-core/icons/ExpoIcon.tsx'
import { default as GraphIcon, getDocumentationProps as getGraphIconProps } from '../../../../../features/app-core/icons/GraphIcon.tsx'
import { default as HomeIcon, getDocumentationProps as getHomeIconProps } from '../../../../../features/app-core/icons/HomeIcon.tsx'
import { default as NextIcon, getDocumentationProps as getNextIconProps } from '../../../../../features/app-core/icons/NextIcon.tsx'
import { default as ReactIcon, getDocumentationProps as getReactIconProps } from '../../../../../features/app-core/icons/ReactIcon.tsx'
import { default as StorybookIcon, getDocumentationProps as getStorybookIconProps } from '../../../../../features/app-core/icons/StorybookIcon.tsx'
// Mocks
import StoryContextProviders from '../../../../../features/app-core/mocks/StoryContextProviders'

<Meta title="features / app-core / icons" />

export const createDocs = (Component) => (args) => {
  return (
    <StoryContextProviders>
      <Component {...args} />
    </StoryContextProviders>
  )
}

## BackIcon

export const BackIconDocs = createDocs(BackIcon)
export const BackIconConfig = aetherStoryDocs({ BackIcon }, getBackIconProps)

`/features/app-core/icons/BackIcon.tsx`
```typescript
import BackIcon from 'app/icons/BackIcon'
```


<Canvas>
    <Story
        name="BackIcon"
        args={BackIconConfig.args}
        argTypes={BackIconConfig.argTypes}
        parameters={{
          backgrounds: {
            default: 'light',
            values: [
              { name: 'light', value: '#F8F8F8' },
              { name: 'dark', value: '#333333' },
            ],
          },
        }}
    >
      {BackIconDocs.bind({})}
    </Story>
</Canvas>

{!isEmpty(BackIconConfig.args) && <ArgsTable story="BackIcon" />}


## ExpoIcon

export const ExpoIconDocs = createDocs(ExpoIcon)
export const ExpoIconConfig = aetherStoryDocs({ ExpoIcon }, getExpoIconProps)

`/features/app-core/icons/ExpoIcon.tsx`
```typescript
import ExpoIcon from 'app/icons/ExpoIcon'
```


<Canvas>
    <Story
        name="ExpoIcon"
        args={ExpoIconConfig.args}
        argTypes={ExpoIconConfig.argTypes}
        parameters={{
          backgrounds: {
            default: 'light',
            values: [
              { name: 'light', value: '#F8F8F8' },
              { name: 'dark', value: '#333333' },
            ],
          },
        }}
    >
      {ExpoIconDocs.bind({})}
    </Story>
</Canvas>

{!isEmpty(ExpoIconConfig.args) && <ArgsTable story="ExpoIcon" />}


## GraphIcon

export const GraphIconDocs = createDocs(GraphIcon)
export const GraphIconConfig = aetherStoryDocs({ GraphIcon }, getGraphIconProps)

`/features/app-core/icons/GraphIcon.tsx`
```typescript
import GraphIcon from 'app/icons/GraphIcon'
```


<Canvas>
    <Story
        name="GraphIcon"
        args={GraphIconConfig.args}
        argTypes={GraphIconConfig.argTypes}
        parameters={{
          backgrounds: {
            default: 'light',
            values: [
              { name: 'light', value: '#F8F8F8' },
              { name: 'dark', value: '#333333' },
            ],
          },
        }}
    >
      {GraphIconDocs.bind({})}
    </Story>
</Canvas>

{!isEmpty(GraphIconConfig.args) && <ArgsTable story="GraphIcon" />}


## HomeIcon

export const HomeIconDocs = createDocs(HomeIcon)
export const HomeIconConfig = aetherStoryDocs({ HomeIcon }, getHomeIconProps)

`/features/app-core/icons/HomeIcon.tsx`
```typescript
import HomeIcon from 'app/icons/HomeIcon'
```


<Canvas>
    <Story
        name="HomeIcon"
        args={HomeIconConfig.args}
        argTypes={HomeIconConfig.argTypes}
        parameters={{
          backgrounds: {
            default: 'light',
            values: [
              { name: 'light', value: '#F8F8F8' },
              { name: 'dark', value: '#333333' },
            ],
          },
        }}
    >
      {HomeIconDocs.bind({})}
    </Story>
</Canvas>

{!isEmpty(HomeIconConfig.args) && <ArgsTable story="HomeIcon" />}


## NextIcon

export const NextIconDocs = createDocs(NextIcon)
export const NextIconConfig = aetherStoryDocs({ NextIcon }, getNextIconProps)

`/features/app-core/icons/NextIcon.tsx`
```typescript
import NextIcon from 'app/icons/NextIcon'
```


<Canvas>
    <Story
        name="NextIcon"
        args={NextIconConfig.args}
        argTypes={NextIconConfig.argTypes}
        parameters={{
          backgrounds: {
            default: 'light',
            values: [
              { name: 'light', value: '#F8F8F8' },
              { name: 'dark', value: '#333333' },
            ],
          },
        }}
    >
      {NextIconDocs.bind({})}
    </Story>
</Canvas>

{!isEmpty(NextIconConfig.args) && <ArgsTable story="NextIcon" />}


## ReactIcon

export const ReactIconDocs = createDocs(ReactIcon)
export const ReactIconConfig = aetherStoryDocs({ ReactIcon }, getReactIconProps)

`/features/app-core/icons/ReactIcon.tsx`
```typescript
import ReactIcon from 'app/icons/ReactIcon'
```


<Canvas>
    <Story
        name="ReactIcon"
        args={ReactIconConfig.args}
        argTypes={ReactIconConfig.argTypes}
        parameters={{
          backgrounds: {
            default: 'light',
            values: [
              { name: 'light', value: '#F8F8F8' },
              { name: 'dark', value: '#333333' },
            ],
          },
        }}
    >
      {ReactIconDocs.bind({})}
    </Story>
</Canvas>

{!isEmpty(ReactIconConfig.args) && <ArgsTable story="ReactIcon" />}


## StorybookIcon

export const StorybookIconDocs = createDocs(StorybookIcon)
export const StorybookIconConfig = aetherStoryDocs({ StorybookIcon }, getStorybookIconProps)

`/features/app-core/icons/StorybookIcon.tsx`
```typescript
import StorybookIcon from 'app/icons/StorybookIcon'
```


<Canvas>
    <Story
        name="StorybookIcon"
        args={StorybookIconConfig.args}
        argTypes={StorybookIconConfig.argTypes}
        parameters={{
          backgrounds: {
            default: 'light',
            values: [
              { name: 'light', value: '#F8F8F8' },
              { name: 'dark', value: '#333333' },
            ],
          },
        }}
    >
      {StorybookIconDocs.bind({})}
    </Story>
</Canvas>

{!isEmpty(StorybookIconConfig.args) && <ArgsTable story="StorybookIcon" />}









---

> ⚙️ Automatically generated by the `yarn document-components` script.

