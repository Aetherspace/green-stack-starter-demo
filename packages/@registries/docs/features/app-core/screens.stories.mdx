import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs'
import { aetherStoryDocs } from 'aetherspace/schemas'
import { isEmpty } from 'aetherspace/utils/commonUtils'
// -i- Auto generated with "yarn document-components"
// Components hooking into the storybook auto-documentation script
import { default as AuthorScreen, getDocumentationProps as getAuthorScreenProps } from '../../../../../features/app-core/screens/AuthorScreen.tsx'
import { default as HomeScreen, getDocumentationProps as getHomeScreenProps } from '../../../../../features/app-core/screens/HomeScreen.tsx'
// Mocks
import StoryContextProviders from '../../../../../features/app-core/mocks/StoryContextProviders'

<Meta title="features / app-core / screens" />

export const createDocs = (Component) => (args) => {
  return (
    <StoryContextProviders>
      <Component {...args} />
    </StoryContextProviders>
  )
}

## AuthorScreen

export const AuthorScreenDocs = createDocs(AuthorScreen)
export const AuthorScreenConfig = aetherStoryDocs({ AuthorScreen }, getAuthorScreenProps)

```typescript
import AuthorScreen from 'features/app-core/screens/AuthorScreen'
```


<Canvas>
    <Story
        name="AuthorScreen"
        args={AuthorScreenConfig.args}
        argTypes={AuthorScreenConfig.argTypes}
    >
      {AuthorScreenDocs.bind({})}
    </Story>
</Canvas>

{!isEmpty(AuthorScreenConfig.args) && <ArgsTable story="AuthorScreen" />}


## HomeScreen

export const HomeScreenDocs = createDocs(HomeScreen)
export const HomeScreenConfig = aetherStoryDocs({ HomeScreen }, getHomeScreenProps)

```typescript
import HomeScreen from 'features/app-core/screens/HomeScreen'
```


<Canvas>
    <Story
        name="HomeScreen"
        args={HomeScreenConfig.args}
        argTypes={HomeScreenConfig.argTypes}
    >
      {HomeScreenDocs.bind({})}
    </Story>
</Canvas>

{!isEmpty(HomeScreenConfig.args) && <ArgsTable story="HomeScreen" />}




---

> ⚙️ Automatically generated by the `yarn document-components` script.

