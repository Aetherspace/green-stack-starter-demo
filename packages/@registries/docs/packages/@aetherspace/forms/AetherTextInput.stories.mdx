import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs'
import { aetherStoryDocs } from 'aetherspace/schemas/utils'
import { isEmpty } from 'aetherspace/utils/commonUtils'
// -i- Auto generated with "yarn document-components"
// Components hooking into the storybook auto-documentation script
import { default as AetherTextInput, getDocumentationProps as getAetherTextInputProps } from '../../../../../../packages/@aetherspace/forms/AetherTextInput/AetherTextInput.tsx'
// Mocks
import StoryContextProviders from '../../../../../../features/app-core/mocks/StoryContextProviders'

<Meta title="packages / @aetherspace / forms / AetherTextInput" />

export const createDocs = (Component) => (args) => {
  return (
    <StoryContextProviders>
      <Component {...args} />
    </StoryContextProviders>
  )
}

## AetherTextInput

export const AetherTextInputDocs = createDocs(AetherTextInput)
export const AetherTextInputConfig = aetherStoryDocs({ AetherTextInput }, getAetherTextInputProps)

`/packages/@aetherspace/forms/AetherTextInput/AetherTextInput.tsx`
```typescript
import AetherTextInput from 'aetherspace/forms/AetherTextInput'
```


<Canvas>
    <Story
        name="AetherTextInput"
        args={AetherTextInputConfig.args}
        argTypes={AetherTextInputConfig.argTypes}
        parameters={{
          backgrounds: {
            default: 'light',
            values: [
              { name: 'light', value: '#FFFFFF' },
              { name: 'dark', value: '#333333' },
            ],
          },
        }}
    >
      {AetherTextInputDocs.bind({})}
    </Story>
</Canvas>

{!isEmpty(AetherTextInputConfig.args) && <ArgsTable story="AetherTextInput" />}



---

> ⚙️ Automatically generated by the `yarn document-components` script.

