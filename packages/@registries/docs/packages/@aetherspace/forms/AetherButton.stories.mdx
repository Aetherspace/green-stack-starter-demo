import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs'
import { aetherStoryDocs } from 'aetherspace/schemas/utils'
import { isEmpty } from 'aetherspace/utils/commonUtils'
// -i- Auto generated with "yarn document-components"
// Components hooking into the storybook auto-documentation script
import { default as AetherButton, getDocumentationProps as getAetherButtonProps } from '../../../../../../packages/@aetherspace/forms/AetherButton/AetherButton.tsx'
// Mocks
import StoryContextProviders from '../../../../../../features/app-core/mocks/StoryContextProviders'

<Meta title="packages / @aetherspace / forms / AetherButton" />

export const createDocs = (Component) => (args) => {
  return (
    <StoryContextProviders>
      <Component {...args} />
    </StoryContextProviders>
  )
}

## AetherButton

export const AetherButtonDocs = createDocs(AetherButton)
export const AetherButtonConfig = aetherStoryDocs({ AetherButton }, getAetherButtonProps)

`/packages/@aetherspace/forms/AetherButton/AetherButton.tsx`
```typescript
import AetherButton from 'aetherspace/forms/AetherButton'
```


<Canvas>
    <Story
        name="AetherButton"
        args={AetherButtonConfig.args}
        argTypes={AetherButtonConfig.argTypes}
        parameters={{
          backgrounds: {
            default: 'light',
            values: [
              { name: 'light', value: '#FFFFFF' },
              { name: 'dark', value: '#333333' },
            ],
          },
        }}
    >
      {AetherButtonDocs.bind({})}
    </Story>
</Canvas>

{!isEmpty(AetherButtonConfig.args) && <ArgsTable story="AetherButton" />}



---

> ⚙️ Automatically generated by the `yarn document-components` script.

