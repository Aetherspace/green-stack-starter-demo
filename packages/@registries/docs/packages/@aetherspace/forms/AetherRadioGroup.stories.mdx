import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs'
import { aetherStoryDocs } from 'aetherspace/schemas/utils'
import { isEmpty } from 'aetherspace/utils/commonUtils'
// -i- Auto generated with "yarn document-components"
// Components hooking into the storybook auto-documentation script
import { default as AetherRadioGroup, getDocumentationProps as getAetherRadioGroupProps } from '../../../../../../packages/@aetherspace/forms/AetherRadioGroup/AetherRadioGroup.tsx'
import { default as AetherRadioOption, getDocumentationProps as getAetherRadioOptionProps } from '../../../../../../packages/@aetherspace/forms/AetherRadioGroup/AetherRadioOption.tsx'
// Mocks
import StoryContextProviders from '../../../../../../features/app-core/mocks/StoryContextProviders'

<Meta title="packages / @aetherspace / forms / AetherRadioGroup" />

export const createDocs = (Component) => (args) => {
  return (
    <StoryContextProviders>
      <Component {...args} />
    </StoryContextProviders>
  )
}

## AetherRadioGroup

export const AetherRadioGroupDocs = createDocs(AetherRadioGroup)
export const AetherRadioGroupConfig = aetherStoryDocs({ AetherRadioGroup }, getAetherRadioGroupProps)

`/packages/@aetherspace/forms/AetherRadioGroup/AetherRadioGroup.tsx`
```typescript
import AetherRadioGroup from 'aetherspace/forms/AetherRadioGroup'
```


<Canvas>
    <Story
        name="AetherRadioGroup"
        args={AetherRadioGroupConfig.args}
        argTypes={AetherRadioGroupConfig.argTypes}
        parameters={{
          backgrounds: {
            default: 'light',
            values: [
              { name: 'light', value: '#FFFFFF' },
              { name: 'dark', value: '#333333' },
            ],
          },
        }}
    >
      {AetherRadioGroupDocs.bind({})}
    </Story>
</Canvas>

{!isEmpty(AetherRadioGroupConfig.args) && <ArgsTable story="AetherRadioGroup" />}


## AetherRadioOption

export const AetherRadioOptionDocs = createDocs(AetherRadioOption)
export const AetherRadioOptionConfig = aetherStoryDocs({ AetherRadioOption }, getAetherRadioOptionProps)

`/packages/@aetherspace/forms/AetherRadioGroup/AetherRadioOption.tsx`
```typescript
import AetherRadioOption from 'aetherspace/forms/AetherRadioGroup/AetherRadioOption'
```


<Canvas>
    <Story
        name="AetherRadioOption"
        args={AetherRadioOptionConfig.args}
        argTypes={AetherRadioOptionConfig.argTypes}
        parameters={{
          backgrounds: {
            default: 'light',
            values: [
              { name: 'light', value: '#FFFFFF' },
              { name: 'dark', value: '#333333' },
            ],
          },
        }}
    >
      {AetherRadioOptionDocs.bind({})}
    </Story>
</Canvas>

{!isEmpty(AetherRadioOptionConfig.args) && <ArgsTable story="AetherRadioOption" />}




---

> ⚙️ Automatically generated by the `yarn document-components` script.

