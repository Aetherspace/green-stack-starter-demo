import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs'
import { aetherStoryDocs } from 'aetherspace/schemas/utils'
import { isEmpty } from 'aetherspace/utils/commonUtils'
// -i- Auto generated with "yarn document-components"
// Components hooking into the storybook auto-documentation script
import { default as AetherView, getDocumentationProps as getAetherViewProps } from '../../../../../../packages/@aetherspace/primitives/AetherView/AetherView.tsx'
// Mocks
import StoryContextProviders from '../../../../../../features/app-core/mocks/StoryContextProviders'

<Meta title="packages / @aetherspace / primitives / AetherView" />

export const createDocs = (Component) => (args) => {
  return (
    <StoryContextProviders>
      <Component {...args} />
    </StoryContextProviders>
  )
}

## AetherView

export const AetherViewDocs = createDocs(AetherView)
export const AetherViewConfig = aetherStoryDocs({ AetherView }, getAetherViewProps)

`/packages/@aetherspace/primitives/AetherView/AetherView.tsx`
```typescript
import AetherView from 'aetherspace/primitives/AetherView'
```


<Canvas>
    <Story
        name="AetherView"
        args={AetherViewConfig.args}
        argTypes={AetherViewConfig.argTypes}
        parameters={{
          backgrounds: {
            default: 'light',
            values: [
              { name: 'light', value: '#FFFFFF' },
              { name: 'dark', value: '#333333' },
            ],
          },
        }}
    >
      {AetherViewDocs.bind({})}
    </Story>
</Canvas>

{!isEmpty(AetherViewConfig.args) && <ArgsTable story="AetherView" />}



---

> ⚙️ Automatically generated by the `yarn document-components` script.

