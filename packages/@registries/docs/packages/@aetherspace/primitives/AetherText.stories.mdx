import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs'
import { aetherStoryDocs } from 'aetherspace/schemas/utils'
import { isEmpty } from 'aetherspace/utils/commonUtils'
// -i- Auto generated with "yarn document-components"
// Components hooking into the storybook auto-documentation script
import { default as AetherText, getDocumentationProps as getAetherTextProps } from '../../../../../../packages/@aetherspace/primitives/AetherText/AetherText.tsx'
// Mocks
import StoryContextProviders from '../../../../../../features/app-core/mocks/StoryContextProviders'

<Meta title="packages / @aetherspace / primitives / AetherText" />

export const createDocs = (Component) => (args) => {
  return (
    <StoryContextProviders>
      <Component {...args} />
    </StoryContextProviders>
  )
}

## AetherText

export const AetherTextDocs = createDocs(AetherText)
export const AetherTextConfig = aetherStoryDocs({ AetherText }, getAetherTextProps)

`/packages/@aetherspace/primitives/AetherText/AetherText.tsx`
```typescript
import AetherText from 'aetherspace/primitives/AetherText'
```


<Canvas>
    <Story
        name="AetherText"
        args={AetherTextConfig.args}
        argTypes={AetherTextConfig.argTypes}
        parameters={{
          backgrounds: {
            default: 'light',
            values: [
              { name: 'light', value: '#FFFFFF' },
              { name: 'dark', value: '#333333' },
            ],
          },
        }}
    >
      {AetherTextDocs.bind({})}
    </Story>
</Canvas>

{!isEmpty(AetherTextConfig.args) && <ArgsTable story="AetherText" />}



---

> ⚙️ Automatically generated by the `yarn document-components` script.

