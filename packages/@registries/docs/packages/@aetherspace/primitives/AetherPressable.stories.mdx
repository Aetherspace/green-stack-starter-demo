import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs'
import { aetherStoryDocs } from 'aetherspace/schemas/utils'
import { isEmpty } from 'aetherspace/utils/commonUtils'
// -i- Auto generated with "yarn document-components"
// Components hooking into the storybook auto-documentation script
import { default as AetherPressable, getDocumentationProps as getAetherPressableProps } from '../../../../../../packages/@aetherspace/primitives/AetherPressable/AetherPressable.tsx'
// Mocks
import StoryContextProviders from '../../../../../../features/app-core/mocks/StoryContextProviders'

<Meta title="packages / @aetherspace / primitives / AetherPressable" />

export const createDocs = (Component) => (args) => {
  return (
    <StoryContextProviders>
      <Component {...args} />
    </StoryContextProviders>
  )
}

## AetherPressable

export const AetherPressableDocs = createDocs(AetherPressable)
export const AetherPressableConfig = aetherStoryDocs({ AetherPressable }, getAetherPressableProps)

`/packages/@aetherspace/primitives/AetherPressable/AetherPressable.tsx`
```typescript
import AetherPressable from 'aetherspace/primitives/AetherPressable'
```


<Canvas>
    <Story
        name="AetherPressable"
        args={AetherPressableConfig.args}
        argTypes={AetherPressableConfig.argTypes}
        parameters={{
          backgrounds: {
            default: 'light',
            values: [
              { name: 'light', value: '#FFFFFF' },
              { name: 'dark', value: '#333333' },
            ],
          },
        }}
    >
      {AetherPressableDocs.bind({})}
    </Story>
</Canvas>

{!isEmpty(AetherPressableConfig.args) && <ArgsTable story="AetherPressable" />}



---

> ⚙️ Automatically generated by the `yarn document-components` script.

