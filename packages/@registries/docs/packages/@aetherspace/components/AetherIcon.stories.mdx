import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs'
import { aetherStoryDocs } from 'aetherspace/schemas/utils'
import { isEmpty } from 'aetherspace/utils/commonUtils'
// -i- Auto generated with "yarn document-components"
// Components hooking into the storybook auto-documentation script
import { AetherIcon, getDocumentationProps as getAetherIconProps } from '../../../../../../packages/@aetherspace/components/AetherIcon/AetherIcon.tsx'
// Mocks
import StoryContextProviders from '../../../../../../features/app-core/mocks/StoryContextProviders'

<Meta title="packages / @aetherspace / components / AetherIcon" />

export const createDocs = (Component) => (args) => {
  return (
    <StoryContextProviders>
      <Component {...args} />
    </StoryContextProviders>
  )
}

## AetherIcon

export const AetherIconDocs = createDocs(AetherIcon)
export const AetherIconConfig = aetherStoryDocs({ AetherIcon }, getAetherIconProps)

`/packages/@aetherspace/components/AetherIcon/AetherIcon.tsx`
```typescript
import { AetherIcon } from 'aetherspace/components/AetherIcon'
```


<Canvas>
    <Story
        name="AetherIcon"
        args={AetherIconConfig.args}
        argTypes={AetherIconConfig.argTypes}
        parameters={{
          backgrounds: {
            default: 'light',
            values: [
              { name: 'light', value: '#F8F8F8' },
              { name: 'dark', value: '#333333' },
            ],
          },
        }}
    >
      {AetherIconDocs.bind({})}
    </Story>
</Canvas>

{!isEmpty(AetherIconConfig.args) && <ArgsTable story="AetherIcon" />}



---

> ⚙️ Automatically generated by the `yarn document-components` script.

